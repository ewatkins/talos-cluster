---
# Replaces the standalone Tailscale VM: advertises the home subnets onto the
# tailnet and serves as an exit node. Routes and exit-node status must be
# approved in the Tailscale admin console (or auto-approved via ACL tag rules).
apiVersion: tailscale.com/v1alpha1
kind: Connector
metadata:
  name: home-network
spec:
  hostname: talos-subnet-router
  subnetRouter:
    advertiseRoutes:
      - 10.0.0.1/32
      - 10.35.0.0/16
      - 10.40.0.0/16
      - 10.45.0.0/16
      - 10.50.0.0/16
      - 10.69.0.0/16 # pod CIDR — direct pod access over the tailnet
      - 10.96.0.0/16 # service CIDR — reach services by ClusterIP (solanyn model)
  exitNode: true
