---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: pterodactyl
  namespace: network
spec:
  endpoints:
    - ip:
        address: 10.50.0.99
        port: 80
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: pterodactyl
  namespace: network
  annotations:
    external-dns.alpha.kubernetes.io/target: "internal.ewatkins.dev"
spec:
  parentRefs:
    - name: internal
      namespace: network
      sectionName: https
  hostnames: ["pterodactyl.ewatkins.dev"]
  rules:
    - backendRefs:
        - group: gateway.envoyproxy.io
          kind: Backend
          name: pterodactyl
