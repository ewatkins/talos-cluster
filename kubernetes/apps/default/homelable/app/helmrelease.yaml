---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app homelable
  namespace: &namespace default
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  maxHistory: 2
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      strategy: rollback
      retries: 3
  values:
    controllers:
      homelable:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/pouzor/homelable-backend
              tag: 3.1.2@sha256:1d7ffebcbbc2de6363ce6c46009e6dd59be8603fcba75b0c6d9bc473ab104d81
            env:
              TZ: ${TIMEZONE}
              SQLITE_PATH: /app/data/homelab.db
              CORS_ORIGINS: '["https://homelab.ewatkins.dev"]'
              SCANNER_RANGES: '["10.40.0.0/24","10.40.1.0/24"]'
            envFrom:
              - secretRef:
                  name: homelable-secret
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /api/v1/health
                    port: 8000
                  initialDelaySeconds: 15
                  periodSeconds: 10
                  timeoutSeconds: 5
                  failureThreshold: 6
              readiness: *probes
            securityContext:
              # PSS baseline forbids adding NET_RAW; run unprivileged instead —
              # nmap falls back to TCP-connect scans and ping uses ICMP datagram
              # sockets via the ping_group_range sysctl below.
              runAsUser: 1000
              runAsGroup: 1000
              allowPrivilegeEscalation: false
              capabilities:
                drop: ["ALL"]
            resources:
              requests:
                cpu: 10m
              limits:
                memory: 512Mi
          frontend:
            image:
              repository: ghcr.io/pouzor/homelable-frontend
              tag: 3.1.2@sha256:3c2fa58a512042bd2a5e07340d6b7d7660fa00591e09e646e0302d47e7911230
            securityContext:
              allowPrivilegeEscalation: false
            resources:
              requests:
                cpu: 5m
              limits:
                memory: 128Mi
          mcp:
            image:
              repository: ghcr.io/pouzor/homelable-mcp
              tag: 3.1.2@sha256:60f9bdb43c0822730b9e3d88567b8b4bcf1a2bc220c33648fca4e5d0e88ddefc
            env:
              TZ: ${TIMEZONE}
              BACKEND_URL: http://127.0.0.1:8000
            envFrom:
              - secretRef:
                  name: homelable-secret
            securityContext:
              runAsUser: 1000
              runAsGroup: 1000
              allowPrivilegeEscalation: false
              capabilities:
                drop: ["ALL"]
            resources:
              requests:
                cpu: 5m
              limits:
                memory: 256Mi
    defaultPodOptions:
      enableServiceLinks: false
      securityContext:
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        sysctls:
          # Allow unprivileged ICMP echo (ping status checks) without NET_RAW
          - name: net.ipv4.ping_group_range
            value: "0 2147483647"
    service:
      app:
        controller: *app
        ports:
          http:
            port: 80
          api:
            port: 8000
          mcp:
            port: 8001
    persistence:
      data:
        existingClaim: homelable-data
        advancedMounts:
          homelable:
            app:
              - path: /app/data
      nginx-conf:
        type: configMap
        name: homelable-nginx
        advancedMounts:
          homelable:
            frontend:
              - path: /etc/nginx/conf.d/default.conf
                subPath: default.conf
                readOnly: true
